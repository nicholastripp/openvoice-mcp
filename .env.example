# Environment Variables for HA Realtime Voice Assistant
# Copy this file to .env and fill in your actual values

# OpenAI API Configuration
OPENAI_API_KEY=sk-...

# Home Assistant Configuration (OPTIONAL - uncomment to enable)
# The system can run as a pure voice UI without Home Assistant
# Uncomment these lines if you want Home Assistant integration
#HA_URL=http://homeassistant.local:8123
#HA_TOKEN=eyJ0eXAiOiJKV1...
#HA_LANG=en

# Picovoice Configuration (for wake word detection)
# Get your free access key at https://console.picovoice.ai/
PICOVOICE_ACCESS_KEY=your_picovoice_access_key_here

# Optional: Override default voice (Note: Currently voice is set in config.yaml)
OPENAI_VOICE=alloy

# Optional: Override default model (Note: Currently model is set in config.yaml)
# Use production model 'gpt-realtime' instead of deprecated preview models
OPENAI_MODEL=gpt-realtime

# Optional: Custom log level
LOG_LEVEL=INFO

# Optional: Custom configuration file path
CONFIG_FILE=config/config.yaml
PERSONA_FILE=config/persona.ini

# Web UI Authentication (set by installer, uses bcrypt)
# The installer will automatically generate this when you enable web UI security
WEB_UI_PASSWORD_HASH=